---
title: "視線と三角形の交差判定"
category: メモ
tags: [CG, レイトレーシング]
math: true
published: true
---

## 参考

[[MT97]](http://www.graphics.cornell.edu/pubs/1997/MT97.html) (Fast, minimum storage ray-triangle intersection) → [安直レイトレーシング入門](https://www.wakayama-u.ac.jp/~tokoi/lecture/ray/)に反映すること。この論文はレイトレーシングやってる人には基本なんだろうな。

## 視線と三角形の交差の有無と交差点の位置を調べる

$\mathbf{O}$ を起点として $\mathbf{D}$ の方向に向かう視線上の点 $\mathbf{R}\left(t\right)$ は、以下のように定義されます。

$$
\mathbf{R}\left(t\right)=\mathbf{O}+t\mathbf{D}
$$

一方、３点 $\mathbf{V}_0$、$\mathbf{V}_1$、$\mathbf{V}_2$ を頂点とする三角形上の点 $\mathbf{S}\left(u, v\right)$ は、以下のように定義されます。

$$
\mathbf{S}\left(u, v\right)=\left(1-u-v\right)\mathbf{V}_0+u\mathbf{V}_1+v\mathbf{V}_2
$$

この $\left(u, v\right)$ はこの三角形上の重心座標で、これがこの三角形の内部にあるなら $u \geq 0$、$v \geq 0$、かつ $u+v \leq 1$ すなわち $1-u-v \geq 0$ を満たします。なお、この $\left(u, v\right)$ は頂点色や頂点法線ベクトル、テクスチャ座標などの頂点属性の補間に用いることができます。

視線 $\mathbf{R}\left(t\right)$ がこの三角形と交差するかどうかを調べるには、$\mathbf{R}\left(t\right)=\mathbf{S}\left(u, v\right)$ を解きます。

$$
\mathbf{O}+t\mathbf{D}=\left(1-u-v\right)\mathbf{V}_0+u\mathbf{V}_1+v\mathbf{V}_2
$$

これを次のように行列とベクトルの積の形に変形します。

$$
\begin{pmatrix}-\mathbf{D}&\mathbf{V}_1-\mathbf{V}_0&\mathbf{V}_2-\mathbf{V}_0\end{pmatrix}\begin{pmatrix}t\\u\\v\end{pmatrix}=\mathbf{O}-\mathbf{V}_0
$$

この $\mathbf{V}_1-\mathbf{V}_0=\mathbf{E}_1$、$\mathbf{V}_2-\mathbf{V}_0=\mathbf{E}_2$、$\mathbf{O}-\mathbf{V}_0=\mathbf{T}$ とおき、クラメールの公式[^fn1]を用いて上式を解きます。

$$
\begin{pmatrix}
t\\
u\\
v
\end{pmatrix}
=\frac{1}{\begin{vmatrix}-\mathbf{D}&\mathbf{E}_1&\mathbf{E}_2\end{vmatrix}}
\begin{pmatrix}
\begin{vmatrix}\mathbf{T}&\mathbf{E}_1&\mathbf{E}_2\end{vmatrix}\\
\begin{vmatrix}-\mathbf{D}&\mathbf{T}&\mathbf{E}_2\end{vmatrix}\\
\begin{vmatrix}-\mathbf{D}&\mathbf{E}_1&\mathbf{T}\end{vmatrix}
\end{pmatrix}
$$

$t$ は視線 $\mathbf{R}\left(t\right)$ のパラメータですから、これより交点の位置を求めることができます。またスカラー三重積より $\begin{vmatrix}\mathbf{A}&\mathbf{B}&\mathbf{C}\end{vmatrix}=-\left(\mathbf{A}\times\mathbf{C}\right)\cdot\mathbf{B}=-\left(\mathbf{C}\times\mathbf{B}\right)\cdot\mathbf{A}$ なので、上式は次のように書き換えることができます。

$$
\begin{pmatrix}
t\\
u\\
v
\end{pmatrix}
=\frac{1}{\left(\mathbf{D}\times\mathbf{E}_2\right)\cdot\mathbf{E}_1}
\begin{pmatrix}
\left(\mathbf{T}\times\mathbf{E}_1\right)\cdot\mathbf{E}_2\\
\left(\mathbf{D}\times\mathbf{E}_2\right)\cdot\mathbf{T}\\
\left(\mathbf{T}\times\mathbf{E}_1\right)\cdot\mathbf{D}
\end{pmatrix}
=\frac{1}{\mathbf{P}\cdot\mathbf{E}_1}
\begin{pmatrix}
\mathbf{Q}\cdot\mathbf{E}_2\\
\mathbf{P}\cdot\mathbf{T}\\
\mathbf{Q}\cdot\mathbf{D}
\end{pmatrix}
$$

ここで $\mathbf{P}=\mathbf{D}\times\mathbf{E}_2$ および $\mathbf{Q}=\mathbf{T}\times\mathbf{E}_1$ です。

## 法線ベクトルを使う

考えてみれば $\mathbf{E}_1\times\mathbf{E}_2=\mathbf{N}$ はこの三角形の法線ベクトルです。これは陰影付けのときにも使用するので、すべての三角形であらかじめ求めていることも多いように思います。もしこれを利用するなら、上式を次のように書き換えてもいいかもしれません。

$$
\begin{pmatrix}
t\\
u\\
v
\end{pmatrix}
=\frac{-1}{\left(\mathbf{E}_1\times\mathbf{E}_2\right)\cdot\mathbf{D}}
\begin{pmatrix}
\left(\mathbf{E}_1\times\mathbf{E}_2\right)\cdot\mathbf{T}\\
\left(\mathbf{T}\times\mathbf{D}\right)\cdot\mathbf{E}_2\\
-\left(\mathbf{T}\times\mathbf{D}\right)\cdot\mathbf{E}_1
\end{pmatrix}
=\frac{-1}{\mathbf{N}\cdot\mathbf{D}}
\begin{pmatrix}
\mathbf{N}\cdot\mathbf{T}\\
\mathbf{M}\cdot\mathbf{E}_2\\
-\mathbf{M}\cdot\mathbf{E}_1
\end{pmatrix}
$$

ここで $\mathbf{M}=\mathbf{T}\times\mathbf{D}$ です。

ということを考えてみたけど、三角形のデータに法線ベクトルを持たせるときは正規化しちゃうだろうし、SSE やシェーダを使えば外積って簡単に計算できるから、あんまり意味ないなぁ。$\mathbf{N}\cdot\mathbf{D}=0$ になるのは視線と三角形が平行のときで、$t$ の式は視線 $\mathbf{R}\left(t\right)$ を三角形の平面の方程式に代入した $\mathbf{N}\cdot\mathbf{R}\left(t\right)-\mathbf{N}\cdot\mathbf{V}_0=0$ を変形して $t$ を求めたものになるな。当たり前か。

## ラスタライザ野郎

ラスタライザを自分で書く私は[古くさいラスタライザ野郎](http://lucille.atso-net.jp/blog/?p=559)なんだろうな。でも、それならレイトレーシングのレンダラを書いている人はサンプリング野郎だよな。

[^fn1]: 私は「クラメール」じゃなくて「クラーメル」って習った気がする。
